# 应用名称
spring:
  application:
    name: clouddemo2
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      label: master
      enabled: true

# 应用服务 WEB 访问端口
server:
  port: 8091
