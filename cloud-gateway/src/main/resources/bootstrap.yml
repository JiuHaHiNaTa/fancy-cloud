# 应用名称
spring:
  application:
    name: cloud-gateway


server:
  port: 9201

#注册中心设置
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka