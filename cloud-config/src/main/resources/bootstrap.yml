#加密 KEY值设置
encrypt:
  key: 666666

management:
  endpoints:
    web:
      exposure:
        include: '*'

#注册中心设置
eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka
    healthcheck:
      enabled: true
